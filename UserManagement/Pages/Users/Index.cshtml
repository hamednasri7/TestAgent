@page
@model UserManagement.Pages.Users.IndexModel
@{
    ViewData["Title"] = "لیست کاربران";
}

<div class="d-flex justify-content-between align-items-center mb-3">
    <h1>لیست کاربران</h1>
    <a asp-page="Create" class="btn btn-primary">افزودن کاربر</a>
</div>

@if (!string.IsNullOrEmpty(Model.StatusMessage))
{
    <div class="alert alert-success">@Model.StatusMessage</div>
}

@if (!Model.Users.Any())
{
    <div class="alert alert-info">هیچ کاربری ثبت نشده است.</div>
}
else
{
    <table class="table table-striped table-bordered align-middle">
        <thead class="table-light">
            <tr>
                <th>#</th>
                <th>نام</th>
                <th>نام خانوادگی</th>
                <th>سن</th>
                <th>موبایل</th>
                <th class="text-center">عملیات</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var user in Model.Users)
            {
                <tr>
                    <td>@user.Id</td>
                    <td>@user.FirstName</td>
                    <td>@user.LastName</td>
                    <td>@user.Age</td>
                    <td>@user.Mobile</td>
                    <td class="text-center">
                        <a asp-page="Edit" asp-route-id="@user.Id" class="btn btn-sm btn-outline-secondary">ویرایش</a>
                        <a asp-page="Delete" asp-route-id="@user.Id" class="btn btn-sm btn-outline-danger">حذف</a>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
